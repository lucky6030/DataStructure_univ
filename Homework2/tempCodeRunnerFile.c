Stack stack;
    // char ch;

    // for(int i = 0; i<strlen(input); i++) {
    //     Push(&stack, input[i]);
    // }

    // while(!IsEmpty(&stack)) {
    //     ch = Peek(&stack);
    //     printf("%c", ch);
    //     Pop(&stack);
    // }